import alt from 'alt';
import native from 'natives';
/*
import {distance} from './utilities/vector.js';
const player = alt.Player.local;
alt.onServer("Miner:StartWorking", () =>{
    console.log("Basladi.");
    native.freezeEntityPosition(player.scriptID, true);
    native.taskStartScenarioInPlace(player.scriptID, "WORLD_HUMAN_CONST_DRILL", 0, true);
    alt.setTimeout(() => {
        native.freezeEntityPosition(player.scriptID, false); 
        native.clearPedTasks(player.scriptID);
        alt.emitServer("Miner:Succes");
    }, 10000); 
    
});*/ // ! Disabled!
